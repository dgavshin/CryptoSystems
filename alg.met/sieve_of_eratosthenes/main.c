#include "libft.h"
#include <stdio.h>

int *getnprime(int n);

int main()
{
    int n = 30;
    int *arr = getnprime(n);
    printf("array: \n");
    /* while (*arr) */
    /* { */
    /*     ft_putnbr(*(arr++)); */
    /*     ft_putchar('\n'); */
    /* } */
    return (0);
}
